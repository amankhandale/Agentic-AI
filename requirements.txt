# Core / Basics
langchain>=0.3.0
langchain-ollama>=0.3.0
langchain-community>=0.3.0
python-dotenv>=1.0.0
requests>=2.32.0

# Audio input/output
pyaudio>=0.2.14
sounddevice>=0.5.0
numpy>=1.26.0

# Speech-to-Text (STT) - offline, accurate & fast
faster-whisper>=1.0.3

# Text-to-Speech (TTS) - very natural local voices
piper-tts>=1.2.0

# Optional / alternatives (commented out - enable only if needed)
# SpeechRecognition>=3.10.0               # classic, but slower + needs internet for Google
# openai-whisper>=20240930                # original whisper - slower than faster-whisper